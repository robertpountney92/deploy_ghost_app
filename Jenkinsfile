pipeline {
    agent { docker { image 'node:6.3' } }
    stages {
        stage('build') {
            steps {
                sh 'npm --version'
		echo "Hello there this is rob"
	    	echo "Another test"
            }
        }
    }
}
